<p id="notice"><%= notice %></p>

<div class="jumbotron">
  <h2 class="display-3">
    GarageReview is the best way to find reviews on local auto repair shops.
  </h2>
  <p class="lead">
    Explore around and also leave a review of the garages you have been using in order to help you fellow drivers. By leaving review here you minimize the chances for shady repair shops to continue their existence or at least there will be some pressure for them to change the way they deal with customers.
  </p>
</div>

<table class="table table-hover table-sm">
  <thead>
    <tr class="thead-inverse">
      <th>Name</th>
      <th>Address</th>
      <th>Phone</th>
      <th>Website</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @garages.each do |garage| %>
      <tr>
        <td><%= garage.name %></td>
        <td><%= garage.address %></td>
        <td><%= garage.phone %></td>
        <td><%= garage.website %></td>
        <td><%= link_to 'Show', garage %></td>
        <td><%= link_to 'Edit', edit_garage_path(garage) %></td>
        <td><%= link_to 'Destroy', garage, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Garage', new_garage_path %>

<div id="garages-footer">

</div>
